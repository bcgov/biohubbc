/**
 * Identity sources supported/recognized by the database. The SPI Migration only requires a DATABASE source.
 *
 * @export
 * @enum {number}
 */
export enum SYSTEM_IDENTITY_SOURCE {
  DATABASE = 'DATABASE'
}
