import { Knex } from 'knex';

/**
 * Add new columns to survey_sample_period table
 *
 * @export
 * @param {Knex} knex
 * @return {*}  {Promise<void>}
 */
export async function up(knex: Knex): Promise<void> {
  await knex.raw(`
----------------------------------------------------------------------------------------
-- Add new type descriptions to the type table  
----------------------------------------------------------------------------------------

SET search_path=biohub;

UPDATE type SET description = 'Tracking animal movements using radio, GPS, or other remote sensing technologies.' WHERE name = 'Animal captures';

`);
}
