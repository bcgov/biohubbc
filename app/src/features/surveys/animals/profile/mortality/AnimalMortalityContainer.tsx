const AnimalMortalityContainer = () => {
  return (
    <>
      {/* <AnimalMortalityToolbar
        label="Mortality"
        buttonLabel="Add"
        buttonTo={`/admin/projects/${projectId}/surveys/${surveyId}/animals/${selectedAnimal?.survey_critter_id}/capture/create`}
      /> */}
    </>
  );
};

export default AnimalMortalityContainer;
