import Box from '@material-ui/core/Box';
import Typography from '@material-ui/core/Typography';
import React from 'react';

const NewBlock: React.FC = () => {
  return (
    <Box mb={6}>
      <Typography variant="h2">New Block</Typography>
    </Box>
  );
};

export default NewBlock;
