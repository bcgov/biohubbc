apiVersion: v1
kind: Pod
metadata:
  name: {{ include "app.fullname" . }}
  namespace: {{ include "environment.namespace" . }}
  labels:
    {{- include "app.labels" . | nindent 4 }}
    role: setup
  annotations:
    {{- include "app.annotations" . | nindent 4 }}
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "1"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  containers:
    - name: postgresql
      env:
        - name: DB_HOST
          value: {{ include "dbHost" . }}
        - name: DB_ADMIN
          valueFrom:
            secretKeyRef:
              key: database-admin
              name: {{ include "dbSecret" . }}
        - name: DB_ADMIN_PASS
          valueFrom:
            secretKeyRef:
              key: database-admin-password
              name: {{ include "dbSecret" . }}
        - name: DB_USER_API
          valueFrom:
            secretKeyRef:
              key: database-user-api
              name: {{ include "dbSecret" . }}
        - name: DB_USER_API_PASS
          valueFrom:
            secretKeyRef:
              key: database-user-api-password
              name: {{ include "dbSecret" . }}
        - name: DB_DATABASE
          valueFrom:
            secretKeyRef:
              key: database-name
              name: {{ include "dbSecret" . }}
        - name: DB_PORT
          value: '5432'
        - name: CHANGE_VERSION
          value: {{ .Release.Name | quote }}
        - name: NODE_ENV
          value: {{ .Values.app.nodeEnv }}
        - name: VERSION
          value: {{ .Values.environment.name }}-{{ .Chart.AppVersion }}-{{ .Release.Name | quote }}
        - name: DB_SCHEMA
          value: {{ .Values.app.dbSchema }}
      image: "{{ .Values.image.registry }}/{{ .Values.image.image }}:{{ include "app.imageTag" . }}"
      imagePullPolicy: Always
      resources:
        requests:
          cpu: {{ .Values.resources.requests.cpu }}
          memory: {{ .Values.resources.requests.memory }}
        limits:
          cpu: {{ .Values.resources.limits.cpu }}
          memory: {{ .Values.resources.limits.memory }}
  restartPolicy: Never
  activeDeadlineSeconds: 900
  dnsPolicy: ClusterFirst
